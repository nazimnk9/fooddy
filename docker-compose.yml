version: "3.9"

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dawatsrls_frontend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"       # host Nginx proxies to port 4000
    environment:
      - PORT=4000